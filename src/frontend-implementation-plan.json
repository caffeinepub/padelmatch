{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Replace Discover \"Zona\" free-text filter with Uruguay departments dropdown",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Replace the Discover filters \"Zona\" text input with a Select dropdown listing all Uruguay departments plus an 'Any' option, updating filters.zone accordingly without changing the filters shape.",
      "acceptanceCriteria": [
        "In frontend/src/components/DiscoverFilters.tsx, the \"Zona\" field is no longer a text <Input>; it uses the existing Select/SelectItem components.",
        "The zone dropdown includes exactly these Uruguay departments as options: Artigas, Canelones, Cerro Largo, Colonia, Durazno, Flores, Florida, Lavalleja, Maldonado, Montevideo, Paysandú, Río Negro, Rivera, Rocha, Salto, San José, Soriano, Tacuarembó, Treinta y Tres.",
        "Selecting a department updates filters.zone via onFiltersChange with the selected department name as a string.",
        "There is a clear way to represent 'no zone filter' (e.g., an \"All\" / \"Any\" option) that sets filters.zone to an empty string so existing filtering logic continues to work.",
        "DiscoverScreen continues to pass the same filters object shape to useDiscoverCandidates without any backend changes required."
      ],
      "file_operations": [
        {
          "path": "frontend/src/components/DiscoverFilters.tsx",
          "operation": "modify",
          "description": "Replace the \"Zona\" <Input> with the existing shadcn Select/SelectItem UI (use SelectTrigger/SelectValue/SelectContent) and provide a fixed options list containing an explicit 'Any/All' option mapped to empty string plus exactly the 19 Uruguay departments. Ensure onValueChange calls onFiltersChange({ ...filters, zone: selectedValue }) and keeps the filters object shape unchanged. Verify the component's usage instructions before implementing."
        }
      ]
    }
  ]
}